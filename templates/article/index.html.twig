{% extends 'base.html.twig' %}

{% block title %}Liste Articles
{% endblock %}


{% block body %}
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>
	<h1 class="text-center">Liste Articles</h1>

	<div class="container">

		<table class="table table-striped">
			{% for article in articles %}

				<tr>
					<td><img src="{{ asset('images/' ~ article.imagePath)}}" alt="" class="img-list-article">
					</td>
					<td >{{ article.titre|raw}}</td>
					<td>{{ article.description|raw}}</td>
					<td>
						<a href="{{ path('article-detail', {'id': article.id})}}" class="btn btn-dark">Détail</a>
					</td>
				</tr>


			{% endfor %}

		</table>
	</div>

{% endblock %}
