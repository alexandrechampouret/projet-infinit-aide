{% extends 'base-admin.html.twig' %}

{% block title %}Citasion{% endblock %}

{% block body %}

<a href="{{ path('admin_citasion_index') }}" class="btn btn-dark mb-3">Retour a la liste </a>
    <h1>Citasion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ citasion.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ citasion.description }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content-around align-items-start">
    {{ include('admin_citasion/_delete_form.html.twig') }}
    <a href="{{ path('admin_citasion_edit', {'id': citasion.id}) }} " class="btn btn-success">edit</a>

    
{% endblock %}
